# LobPong
A breakout style game set somewhere in the cosmic depths of the universe.

To compile, type "cd " into terminal and then drag the project folder ("LobPong-master") into terminal, then hit return.
Then, type "javac LobPongTest.java" into terminal and hit return.

To execute, type "java LobPongTest" into terminal and hit return.
